import 'package:flutter/material.dart';

Widget _heroAsset(
  String asset, {
  double? width,
  double height = 220,
  BoxFit fit = BoxFit.cover,
  FilterQuality filterQuality = FilterQuality.high,
}) {
  return Image.asset(
    asset,
    width: width ?? double.infinity,
    height: height,
    fit: fit,
    filterQuality: filterQuality,
    errorBuilder: (c, e, s) {
      return Container(
        height: height,
        width: width ?? double.infinity,
        decoration: const BoxDecoration(
          gradient: LinearGradient(
            begin: Alignment.topCenter,
            end: Alignment.bottomCenter,
            colors: [Color(0xFF111216), Color(0xFF0E0F12)],
          ),
        ),
        child: const Center(
          child: Text(
            'Hero image missing',
            style: TextStyle(color: Color(0xFFFF7A00), fontWeight: FontWeight.w700),
          ),
        ),
      );
    },
  );
}

Widget _truckHero({
  required String asset,
  double overlay = 0.55,
  double radius = 16,
}) {
  return ClipRRect(
    borderRadius: BorderRadius.circular(radius),
    child: Stack(
      children: [
        _heroAsset(
          asset,
          width: double.infinity,
          height: 220,
          fit: BoxFit.cover,
          filterQuality: FilterQuality.high,
        ),
        Positioned.fill(
          child: DecoratedBox(
            decoration: BoxDecoration(
              gradient: LinearGradient(
                begin: Alignment.topCenter,
                end: Alignment.bottomCenter,
                colors: [
                  const Color(0xFF000000).withOpacity(overlay * 0.85),
                  const Color(0xFF000000).withOpacity(overlay),
                ],
              ),
            ),
          ),
        ),
        Positioned.fill(
          child: IgnorePointer(
            child: DecoratedBox(
              decoration: BoxDecoration(
                gradient: LinearGradient(
                  begin: Alignment.centerLeft,
                  end: Alignment.centerRight,
                  colors: [
                    const Color(0xFF000000).withOpacity(0.08),
                    Colors.transparent,
                    const Color(0xFF000000).withOpacity(0.08),
                  ],
                ),
              ),
            ),
          ),
        ),
      ],
    ),
  );
}

Widget _spacerGlass({ double h = 12 }) => SizedBox(height: h);

Widget _spacerGlass({double h = 12}) => SizedBox(height: h);
import 'package:flutter/material.dart';

Widget _heroAsset(
  String asset, {
  double? width,
  double height = 220,
  BoxFit fit = BoxFit.cover,
  FilterQuality filterQuality = FilterQuality.high,
}) {
  return Image.asset(
    asset,
    width: width ?? double.infinity,
    height: height,
    fit: fit,
    filterQuality: filterQuality,
    errorBuilder: (c, e, s) {
      return Container(
        height: height,
        width: width ?? double.infinity,
        decoration: const BoxDecoration(
          gradient: LinearGradient(
            begin: Alignment.topCenter,
            end: Alignment.bottomCenter,
            colors: [Color(0xFF111216), Color(0xFF0E0F12)],
          ),
        ),
        child: const Center(
          child: Text(
            "Hero image missing",
            style: TextStyle(color: Color(0xFFFF7A00), fontWeight: FontWeight.w700),
          ),
        ),
      );
    },
  );
}

Widget _truckHero({
  required String asset,
  double overlay = 0.55,
  double radius = 16,
}) {
  return ClipRRect(
    borderRadius: BorderRadius.circular(radius),
    child: Stack(
      children: [
        _heroAsset(
          asset,
          width: double.infinity,
          height: 220,
          fit: BoxFit.cover,
          filterQuality: FilterQuality.high,
        ),
        Positioned.fill(
          child: DecoratedBox(
            decoration: BoxDecoration(
              gradient: LinearGradient(
                begin: Alignment.topCenter,
                end: Alignment.bottomCenter,
                colors: [
                  const Color(0xFF000000).withOpacity(overlay * 0.85),
                  const Color(0xFF000000).withOpacity(overlay),
                ],
              ),
            ),
          ),
        ),
        Positioned.fill(
          child: IgnorePointer(
            child: DecoratedBox(
              decoration: BoxDecoration(
                gradient: LinearGradient(
                  begin: Alignment.centerLeft,
                  end: Alignment.centerRight,
                  colors: [
                    const Color(0xFF000000).withOpacity(0.08),
                    Colors.transparent,
                    const Color(0xFF000000).withOpacity(0.08),
                  ],
                ),
              ),
            ),
          ),
        ),
      ],
    ),
  );
}

Widget _spacerGlass({double h = 12}) => SizedBox(height: h);

Widget _spacerGlass({double h = 12}) => SizedBox(height: h);
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import 'package:google_fonts/google_fonts.dart';
// import 'package:divergent_alliance/screens/weather_center_pro.dart';
import 'package:divergent_alliance/screens/weather_center_gate.dart' as gate;

Widget _heroAsset(
  String asset, {
  double? width,
  double height = 220,
  BoxFit fit = BoxFit.cover,
  FilterQuality filterQuality = FilterQuality.high,
}) {
  return Image.asset(
    asset,
    width: width ?? double.infinity,
    height: height,
    fit: fit,
    filterQuality: filterQuality,
    errorBuilder: (c, e, s) {
      return Container(
        height: height,
        width: width ?? double.infinity,
        decoration: const BoxDecoration(
          gradient: LinearGradient(
            begin: Alignment.topCenter,
            end: Alignment.bottomCenter,
            colors: [Color(0xFF111216), Color(0xFF0E0F12)],
          ),
        ),
        child: const Center(
          child: Text(
            "Hero image missing",
            style: TextStyle(color: Color(0xFFFF7A00), fontWeight: FontWeight.w700),
          ),
        ),
      );
    },
  );
}



  return _heroAsset(
    asset,
    width: double.infinity,
    height: height,
    fit: BoxFit.cover,
    filterQuality: FilterQuality.high,
    errorBuilder: (c,e,s){
      return Container(
        height: height,
        width: double.infinity,
        decoration: const BoxDecoration(
          gradient: LinearGradient(
            colors: [Color(0xFF111216), Color(0xFF0E0F12)],
            begin: Alignment.topCenter, end: Alignment.bottomCenter),
        child: const Center(
          child: Text("Hero image missing",
            style: TextStyle(color: Color(0xFFFF7A00), fontWeight: FontWeight.w700)),
    },
}

Widget _truckHero({
  required String asset,
  double overlay = 0.55, // strength of black glass
  double radius = 16,
  return ClipRRect(
    borderRadius: BorderRadius.circular(radius),
    child: Stack(
      children: [
        _heroAsset(
          asset,
          width: double.infinity,
          height: 220,
          fit: BoxFit.cover,
          filterQuality: FilterQuality.high,
        Positioned.fill(
          child: DecoratedBox(
            decoration: BoxDecoration(
              gradient: LinearGradient(
                begin: Alignment.topCenter,
                end: Alignment.bottomCenter,
                colors: [
                  const Color(0xFF000000).withOpacity(overlay * 0.85),
                  const Color(0xFF000000).withOpacity(overlay),
                ],
        Positioned.fill(
          child: IgnorePointer(
            child: DecoratedBox(
              decoration: BoxDecoration(
                gradient: LinearGradient(
                  begin: Alignment.centerLeft,
                  end: Alignment.centerRight,
                  colors: [
                    const Color(0xFF000000).withOpacity(0.08),
                    Colors.transparent,
                    const Color(0xFF000000).withOpacity(0.08),
                  ],
      ],
}

Widget _spacerGlass({double h = 12}) => SizedBox(height: h);